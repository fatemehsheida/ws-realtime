# 📡 راهنمای اتصال برای چت با دوست

## روش 1: استفاده از IP محلی (در شبکه محلی)

### مرحله 1: پیدا کردن IP آدرس کامپیوتر شما

#### در Windows:
1. کلید `Win + R` را بزنید
2. `cmd` را تایپ کنید و Enter بزنید
3. دستور زیر را اجرا کنید:
   ```bash
   ipconfig
   ```
4. به دنبال `IPv4 Address` در بخش `Wireless LAN adapter Wi-Fi` یا `Ethernet adapter` بگردید
5. IP شما چیزی شبیه `192.168.1.100` خواهد بود

#### در Mac/Linux:
```bash
ifconfig | grep "inet "
```
یا
```bash
ip addr show
```

### مرحله 2: تنظیم سرور

1. فایل `server.js` را باز کنید
2. مطمئن شوید که سرور روی `0.0.0.0` گوش می‌دهد (نه فقط localhost)

### مرحله 3: اجرای سرور

```bash
npm run dev
```

### مرحله 4: اتصال دوست

دوستان شما باید:
1. مرورگر را باز کنند
2. به آدرس `http://YOUR_IP:3000` بروند (مثلاً `http://192.168.1.100:3000`)
3. در Console مرورگر (F12) این کد را اجرا کنند:
   ```javascript
   localStorage.setItem('chat-server-url', 'http://YOUR_IP:3001');
   location.reload();
   ```
   (مثلاً `http://192.168.1.100:3001`)

## روش 2: استفاده از ngrok (برای اتصال از اینترنت)

### مرحله 1: نصب ngrok

از [ngrok.com](https://ngrok.com) دانلود و نصب کنید

### مرحله 2: اجرای ngrok

```bash
ngrok http 3001
```

### مرحله 3: استفاده از URL ارائه شده

ngrok یک URL مثل `https://abc123.ngrok.io` به شما می‌دهد

### مرحله 4: تنظیم کلاینت

در Console مرورگر (F12) این کد را اجرا کنید:
```javascript
localStorage.setItem('chat-server-url', 'https://YOUR_NGROK_URL');
location.reload();
```

## روش 3: استفاده از Cloud (برای استفاده دائمی)

### گزینه‌های رایگان:
- **Railway**: [railway.app](https://railway.app)
- **Render**: [render.com](https://render.com)
- **Fly.io**: [fly.io](https://fly.io)

### مراحل:
1. پروژه را به GitHub push کنید
2. در سرویس cloud یک پروژه جدید بسازید
3. از GitHub connect کنید
4. Environment variables را تنظیم کنید
5. Deploy کنید

## نکات مهم:

1. **فایروال**: مطمئن شوید که پورت‌های 3000 و 3001 در فایروال باز هستند
2. **شبکه**: در روش 1، هر دو باید در یک شبکه Wi-Fi باشند
3. **HTTPS**: برای استفاده از میکروفون در مرورگر، ممکن است نیاز به HTTPS باشد
4. **امنیت**: در production، CORS را محدود کنید و از authentication استفاده کنید

## تست اتصال:

برای تست اینکه آیا سرور در دسترس است:
```bash
curl http://YOUR_IP:3001
```

یا در مرورگر:
```
http://YOUR_IP:3001/socket.io/
```

اگر پیام `{"code":0,"message":"Transport unknown"}` را دیدید، سرور کار می‌کند! ✅

